scrape_configs:
  - job_name: myapp-test
    #🔥采集请求的url地址（因为使用的是直接采集的方式：在项目中编写程序对要监测的客户端进行埋点采集数据）
    metrics_path: /metrics
    # 具体要访问服务的地址
    static_configs:
      - targets:
          - 192.168.117.80:1234